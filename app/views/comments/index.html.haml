%p#notice= notice
%h1 Listing Comments
%table
  %thead
    %tr
      %th Author
      %th Body
      %th Post
      %th{:colspan => "3"}
  %tbody
    - @comments.each do |comment|
      %tr
        %td= comment.author
        %td= comment.body
        %td= link_to 'Show', [comment.post, comment]
        %td= link_to 'Edit', edit_post_comment_path(comment.post, comment)
        %td= link_to 'Destroy', [comment.post, comment], method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'Back', posts_path
|
= link_to 'New Comment', new_post_comment_path
